#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--)
    {
        int x;
        cin>>x;
        cout<<x<<"\n";  // if y>=x then it will give the min=x in every case y>=x so y=x in this case min of(x,y) will be maximum.

    }
    return 0;

}
